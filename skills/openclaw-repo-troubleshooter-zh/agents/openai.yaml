interface:
  display_name: "OpenClaw 仓库排障助手"
  short_description: "用真实执行结果分析仓库报错并输出结构化报告。"
  default_prompt: "Use $openclaw-repo-troubleshooter-zh to analyze a real repository error and produce structured troubleshooting output."

policy:
  allow_implicit_invocation: true

